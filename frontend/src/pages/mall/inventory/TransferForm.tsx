/**
 * ERP 仓 → mall 仓 调拨
 *
 * 双边事务：扣 ERP inventory + 加 mall_inventory + 双边流水
 * POST /api/mall/admin/inventory/transfer
 *
 * TODO(M2)
 */
export default function MallTransferForm() {
  return <div>库存调拨（TODO M2）</div>;
}
