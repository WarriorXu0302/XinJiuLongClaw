/**
 * C 端用户列表
 *
 * 筛选：status (active/disabled/inactive_archived) / 推荐人 / 日期
 * 显示：昵称、手机、推荐人、状态、累计订单、累计 GMV
 *
 * TODO(M5): GET /api/mall/admin/users?user_type=consumer
 */
export default function ConsumerList() {
  return <div>C 端用户列表（TODO M5）</div>;
}
