# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
alembic>=1.14.0

# Data Validation
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Logging
loguru>=0.7.0

# Redis
redis>=5.0.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.3.0  # passlib 1.7 不兼容 bcrypt 5.x（触发 72-byte 自检 ValueError）
python-multipart>=0.0.12

# HTTP Client
httpx>=0.27.0

# Date/Time
python-dateutil>=2.9.0

# Environment
python-dotenv>=1.0.0

# MCP protocol bridge (Streamable-HTTP for Claude Code / Bisheng)
mcp>=1.0.0

# Policy Excel 两轮对账
openpyxl>=3.1.0

# 定时任务（mall housekeeping：超时未接单扫描 / 用户归档 / 订单折损 / 日志清理）
apscheduler>=3.10.0

# 邀请码二维码生成（SVG 输出，无 Pillow 依赖）
qrcode>=7.4.0
