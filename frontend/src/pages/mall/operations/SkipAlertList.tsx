/**
 * 跳单告警列表
 *
 * Tabs：open / resolved / dismissed
 * 点详情：展开所有 skip_logs
 * 处理：resolved / dismissed，填 note
 * 申诉审核入口（/appeals）
 *
 * TODO(M5)
 */
export default function SkipAlertList() {
  return <div>跳单告警（TODO M5）</div>;
}
