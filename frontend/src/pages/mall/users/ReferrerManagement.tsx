/**
 * 推荐关系管理
 *
 * 可视化：某业务员名下所有 consumer（树状/列表）
 * 批量换绑：选多个客户 → 换到另一业务员，理由必填
 *
 * 权限：仅 ERP 后台 admin/boss；小程序业务员工作台无此入口（403）
 *
 * TODO(M5)
 */
export default function ReferrerManagement() {
  return <div>推荐关系管理（TODO M5）</div>;
}
