/**
 * 登录日志列表
 *
 * 列：user / 登录时间 / IP / UA / device_info / login_method / client_app
 * 过滤：user / 日期 / method / ip
 *
 * TODO(M5): GET /api/mall/admin/login-logs
 */
export default function LoginLogList() {
  return <div>登录日志（TODO M5）</div>;
}
