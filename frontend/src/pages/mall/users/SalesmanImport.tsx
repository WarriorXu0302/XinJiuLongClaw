/**
 * 批量导入业务员（Excel）
 *
 * 模板：username / linked_employee_no / brand_code / phone / nickname
 * 后端：POST /api/mall/admin/salesmen/bulk-import
 *
 * TODO(M5)
 */
export default function SalesmanImport() {
  return <div>批量导入业务员（TODO M5）</div>;
}
