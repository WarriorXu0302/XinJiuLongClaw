/**
 * 登录频率统计
 *
 * 按 user 聚合 N 天登录次数，降序前 100；> 50/天 标红；识别频繁查价行为
 * GET /api/mall/admin/login-logs/stats
 *
 * TODO(M5)
 */
export default function LoginLogStats() {
  return <div>登录频率统计（TODO M5）</div>;
}
