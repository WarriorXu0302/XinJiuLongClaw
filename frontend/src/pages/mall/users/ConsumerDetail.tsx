/**
 * C 端用户详情
 *
 * Tabs：
 *   基础信息 / 地址列表 / 订单历史 / 登录日志（最近 30 条）
 *
 * 操作：
 *   换绑推荐人（PUT /users/{id}/referrer，必填 reason）
 *   手动归档 / 启用 / 禁用（必填 reason，审计）
 *
 * TODO(M5)
 */
export default function ConsumerDetail() {
  return <div>C 端用户详情（TODO M5）</div>;
}
